import { CheckBox } from '@mui/icons-material';
import { Box, Button, FormControl, FormControlLabel, Grid2, Input, InputLabel, MenuItem, Select, TextField, Typography } from '@mui/material';
import { DatePicker, LocalizationProvider } from '@mui/x-date-pickers';
import { AdapterDayjs } from '@mui/x-date-pickers/AdapterDayjs';
import { ErrorMessage, Field, Form, Formik } from 'formik';
import React, { useEffect, useState } from 'react';
import { userRegisterValidations } from '../../lib/validations/user-register-validations';
import axios from 'axios';


const UserRegister = () => {

    return (
        <>
            
        </>
    );
};

export default UserRegister;